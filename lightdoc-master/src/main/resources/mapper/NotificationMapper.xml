<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lightdoc.mapper.NotificationMapper">

    <!-- 批量标记为已读 -->
    <update id="markAsReadBatch">
        UPDATE notifications
        SET is_read = 1, updated_at = NOW()
        WHERE user_id = #{userId}
          AND id IN
        <foreach collection="notificationIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
          AND deleted = 0
    </update>

    <!-- 统计未读通知数量 -->
    <select id="countUnreadByUserId" resultType="java.lang.Long">
        SELECT COUNT(*)
        FROM notifications
        WHERE user_id = #{userId}
          AND is_read = 0
          AND deleted = 0
    </select>

    <!-- 统计发送者未读通知数量 -->
    <select id="countSentUnreadBySenderId" resultType="java.lang.Long">
        SELECT COUNT(*)
        FROM notifications
        WHERE sender_id = #{senderId}
          AND is_read = 0
          AND deleted = 0
    </select>

</mapper>